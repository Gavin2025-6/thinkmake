{
  "name": "thinkmake",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate --schema=prisma/schema.prisma && prisma migrate deploy --schema=prisma/schema.prisma && next build",
    "start": "./node_modules/.bin/next start -H 0.0.0.0 -p ${PORT:-3000}"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "@prisma/client": "^5.22.0",
    "next": "^14.2.0",
    "prisma": "^5.22.0",
    "react": "^18",
    "react-dom": "^18",
    "resend": "^6.12.0"
  }
}
